// Code generated by goa v3.7.4, DO NOT EDIT.
//
// hello HTTP client CLI support package
//
// Command:
// $ goa gen hello-goakit/design

package cli

import (
	"flag"
	"fmt"
	helloc "hello-goakit/gen/http/hello/client"
	"net/http"
	"os"

	"github.com/go-kit/kit/endpoint"
	goahttp "goa.design/goa/v3/http"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `hello hello
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` hello hello --name "Mollitia sint omnis modi consequatur harum."` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (endpoint.Endpoint, interface{}, error) {
	var (
		helloFlags = flag.NewFlagSet("hello", flag.ContinueOnError)

		helloHelloFlags    = flag.NewFlagSet("hello", flag.ExitOnError)
		helloHelloNameFlag = helloHelloFlags.String("name", "REQUIRED", "name")
	)
	helloFlags.Usage = helloUsage
	helloHelloFlags.Usage = helloHelloUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "hello":
			svcf = helloFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "hello":
			switch epn {
			case "hello":
				epf = helloHelloFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint endpoint.Endpoint
		err      error
	)
	{
		switch svcn {
		case "hello":
			c := helloc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "hello":
				endpoint = c.Hello()
				data, err = helloc.BuildHelloPayload(*helloHelloNameFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// helloUsage displays the usage of the hello command and its subcommands.
func helloUsage() {
	fmt.Fprintf(os.Stderr, `The hello service says hello.
Usage:
    %[1]s [globalflags] hello COMMAND [flags]

COMMAND:
    hello: Hello implements hello.

Additional help:
    %[1]s hello COMMAND --help
`, os.Args[0])
}
func helloHelloUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] hello hello -name STRING

Hello implements hello.
    -name STRING: name

Example:
    %[1]s hello hello --name "Mollitia sint omnis modi consequatur harum."
`, os.Args[0])
}
